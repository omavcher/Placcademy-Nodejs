<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Internships</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            color: #333;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .header button {
            background: linear-gradient(135deg, #FB4B04, #e94e0f);
            border: none;
            color: white;
            padding: 12px 25px;
            font-size: 16px;
            margin: 5px;
            cursor: pointer;
            border-radius: 25px;
            transition: background 0.3s, transform 0.3s;
        }
        .header button:hover {
            background: linear-gradient(135deg, #e94e0f, #FB4B04);
            transform: scale(1.05);
        }
        .back-btn {
            background: #FB4B04;
            font-size: 14px;
            text-transform: uppercase;
        }
        .back-btn:hover {
            background: #e94e0f;
        }
        .msg-n-box {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 20px;
            color: white;
            position: relative;
        }
        .msg-n-box h1 {
            margin: 0;
            font-size: 16px;
        }
        .msg-n-box .close-btn {
            background: transparent;
            border: none;
            color: white;
            font-size: 20px;
            cursor: pointer;
            position: absolute;
            top: 10px;
            right: 10px;
        }
        .card {
            background: #ffffff;
            border-radius: 10px;
            box-shadow: 0 6px 12px rgba(0,0,0,0.1);
            margin-bottom: 20px;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            position: relative;
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 24px rgba(0,0,0,0.2);
        }
        .card img {
            border-radius: 10px;
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
        .card-content {
            margin-top: 15px;
            width: 100%;
        }
        .card h2 {
            margin: 0 0 10px;
            font-size: 1.6em;
            color: #FB4B04;
        }
        .card p {
            margin: 8px 0;
            color: #555;
        }
        .card .buttons {
            margin-top: 15px;
        }
        .card .buttons button {
            background: #FB4B04;
            color: white;
            border: none;
            padding: 10px 20px;
            margin: 5px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 5px;
            transition: background 0.3s, transform 0.3s;
        }
        .card .buttons button:hover {
            background: #e94e0f;
            transform: scale(1.05);
        }
        @media (max-width: 768px) {
            .card img {
                height: 150px;
            }
            .card h2 {
                font-size: 1.4em;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <button class="back-btn" onclick="window.history.back()">Back</button>
            <h1>Internship Listings</h1>
        </div>

        <% if (success && success.length) { %>
        <div style="background-color: #00a113;" class="msg-n-box">
            <div id="success-msg">
                <h1><%= success %></h1>
            </div>
            <button class="close-btn" onclick="this.parentElement.style.display='none'">X</button>
        </div>
        <% } else if (error && error.length) { %>
        <div style="background-color: #ff0b02;" class="msg-n-box">
            <div id="error-msg">
                <h1><%= error %></h1>
            </div>
            <button class="close-btn" onclick="this.parentElement.style.display='none'">X</button>
        </div>
        <% } %>

        <% internships.forEach(internship => { %>
        <div class="card">
            <img src="<%= internship.picture %>" alt="<%= internship.internshipname %>">
            <div class="card-content">
                <h2><%= internship.internshipname %></h2>
                <p>Price: $<%= internship.price %></p>
                <p>Number of Users Bought: <%= internship.numberofusersbuy %></p>
                <p>Description: <%= internship.internshipdescription %></p>
                <p>Details: <%= internship.internshipdetails %></p>

                <div class="buttons">
                    <a href="/admin/internships/edit/<%= internship._id %>"><button>Edit</button></a>
                    <a href="/admin/internships/delete/<%= internship._id %>"><button>Delete</button></a>
                </div>
            </div>
        </div>
        <% }); %>
    </div>
</body>
</html>
